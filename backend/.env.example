# Backend Environment Configuration Template
# Copy this file to .env and fill in your actual values

# JWT Secret - Generate a secure random string (at least 32 characters)
# You can generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secure-jwt-secret-here-at-least-32-characters

# AWS Configuration
MY_AWS_REGION=us-east-1
MY_AWS_PROFILE=default

# DynamoDB Table Names (will be auto-generated based on stage)
# These are set automatically by serverless.yml - no need to change